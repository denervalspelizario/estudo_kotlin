import 'pessoa.dart'; // importando a classe pessoa

main() {
  var joao = Pessoa('Denerval', 'Pelizario'); // chamando a classe Pessoa o new é opcional
  print(joao.nomeCompleto()); // imprimindo o metodo nomeCompleto
}